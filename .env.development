# Local review uses the local Django API so changes can be checked before
# relying on the deployed Railway backend.
VITE_API_BASE=/api/v1
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# To test against production instead, set:
# VITE_API_PROXY_TARGET=https://web-production-0f6b5.up.railway.app
