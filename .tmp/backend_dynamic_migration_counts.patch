diff --git a/charts/migrations/0004_load_master_dataset.py b/charts/migrations/0004_load_master_dataset.py
--- a/charts/migrations/0004_load_master_dataset.py
+++ b/charts/migrations/0004_load_master_dataset.py
@@ -9,7 +9,8 @@ def load_master_dataset(apps, schema_editor):
     workbook = Path(settings.BASE_DIR) / "charts" / "seed_data" / "Ngoma_Charts_MASTER.xlsx"
     report = import_master_workbook(apps, workbook, clear=True)
-    if report["combined_rows"] != 900:
-        raise RuntimeError("Master dataset migration did not import exactly 900 Combined rows")
+    expected = 50 * 2 * len(report["months"])
+    if report["combined_rows"] != expected:
+        raise RuntimeError(f"Master dataset migration did not import exactly {expected} Combined rows")
 
 
 class Migration(migrations.Migration):
diff --git a/charts/migrations/0005_reload_verified_metadata.py b/charts/migrations/0005_reload_verified_metadata.py
--- a/charts/migrations/0005_reload_verified_metadata.py
+++ b/charts/migrations/0005_reload_verified_metadata.py
@@ -9,6 +9,7 @@ def reload_verified_metadata(apps, schema_editor):
     workbook = Path(settings.BASE_DIR) / "charts" / "seed_data" / "Ngoma_Charts_MASTER.xlsx"
     report = import_master_workbook(apps, workbook, clear=True)
-    if report["combined_rows"] != 900:
+    expected = 50 * 2 * len(report["months"])
+    if report["combined_rows"] != expected:
         raise RuntimeError("Verified metadata reload did not import all Combined chart rows")
 
 
diff --git a/charts/migrations/0006_reload_canonical_chart_data.py b/charts/migrations/0006_reload_canonical_chart_data.py
--- a/charts/migrations/0006_reload_canonical_chart_data.py
+++ b/charts/migrations/0006_reload_canonical_chart_data.py
@@ -9,6 +9,7 @@ def reload_canonical_chart_data(apps, schema_editor):
     workbook = Path(settings.BASE_DIR) / "charts" / "seed_data" / "Ngoma_Charts_MASTER.xlsx"
     report = import_master_workbook(apps, workbook, clear=True)
-    if report["combined_rows"] != 900:
+    expected = 50 * 2 * len(report["months"])
+    if report["combined_rows"] != expected:
         raise RuntimeError("Canonical chart reload did not import all Combined chart rows")
 
 
diff --git a/charts/migrations/0007_reload_top_50_movements.py b/charts/migrations/0007_reload_top_50_movements.py
--- a/charts/migrations/0007_reload_top_50_movements.py
+++ b/charts/migrations/0007_reload_top_50_movements.py
@@ -9,6 +9,7 @@ def reload_chart_data(apps, schema_editor):
     workbook = Path(settings.BASE_DIR) / "charts" / "seed_data" / "Ngoma_Charts_MASTER.xlsx"
     report = import_master_workbook(apps, workbook, clear=True)
-    if report["combined_rows"] != 900:
+    expected = 50 * 2 * len(report["months"])
+    if report["combined_rows"] != expected:
         raise RuntimeError("Top 50 chart reload did not import all Combined chart rows")
 
 
diff --git a/charts/migrations/0008_sync_frontend_chart_data.py b/charts/migrations/0008_sync_frontend_chart_data.py
--- a/charts/migrations/0008_sync_frontend_chart_data.py
+++ b/charts/migrations/0008_sync_frontend_chart_data.py
@@ -9,6 +9,7 @@ def sync_frontend_chart_data(apps, schema_editor):
     workbook = Path(settings.BASE_DIR) / "charts" / "seed_data" / "Ngoma_Charts_MASTER.xlsx"
     report = import_master_workbook(apps, workbook, clear=True)
-    if report["combined_rows"] != 900:
+    expected = 50 * 2 * len(report["months"])
+    if report["combined_rows"] != expected:
         raise RuntimeError("Frontend chart sync did not import all Combined chart rows")
 
 
