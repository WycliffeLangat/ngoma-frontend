{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": [
      "python manage.py migrate --noinput"
    ],
    "startCommand": "python manage.py collectstatic --noinput && gunicorn ngoma_backend.wsgi --workers 2 --timeout 900 --log-file -"
  }
}
